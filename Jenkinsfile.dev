pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'git checkout HEAD /var/www/qa.clubhabbo.com/'
                sh 'npm --prefix /var/www/qa.clubhabbo.com/ install /var/www/qa.clubhabbo.com/'
                sh 'npm --prefix /var/www/qa.clubhabbo.com/ run development'
            }
        }
    }
}
