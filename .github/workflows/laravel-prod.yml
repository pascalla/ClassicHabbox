name: Laravel Prod Deploy

on: [push]

jobs:
  laravel-compile:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v1
      with:
        path: '/var/www/ch'
    - name: Migrate
      run: php artisan migrate
    - name: Compile Assets
      run: npm run prod
