name: Laravel Prod Deploy

on: [push]

jobs:
  laravel-compile:
    runs-on: self-hosted
    steps:
    - name: Migrate
      run: php artisan migrate
    - name: Compile Assets
      run: npm run prod
